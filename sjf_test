# arrivalTime -> thoi diem tien trinh xuat hien
# burstTime ->  thoi gian CPU can de xu li tien trinh
# id -> stt
# remainingTime -> thoi gian con lai (luc dau = burstTime, giam dan khi tien trinh duoc chay)

#proccesses: list chua cac object (P1,P2,P3,...)

class proccess:
    # ham khoi tao
    def __init__(self,id,arrivalTime,burstTime):
        self.id = id
        self.arrivalTime = arrivalTime
        self.burstTimme = burstTime
        self.remainingTime = burstTime

    def sjf (processes):
        currentTime, completed = 0,0
        while completed<len(processes):
            index=-1
            for i,p in enumerate(processes):
                if p.arrivalTime <= currentTime and p.remainingTime>0 & (index==-1 | p.remainingTime<processes[index].remainingTime):
                    index=1
                    
